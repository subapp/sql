select *
from users U
       Inner join tableName t0 ON (t0.cnt / 10 - 3) + 1 = sum(distinct u.cnt)
                                    || round(pi(), 2) = 3.14
                                         && (t0.cnt / 10) + 3 = 1

where
      (
          (u.id * 1 * 2)
      ) > SUM(DISTINCT U.cnt)
      AND (
        t0.id <= t1.subId
        OR t1.id >= 1
      )
     &&
      (t0.id / 10 + 3 > 1) and
      (t0.subId / 10 + 3) > 1
   or (u.id * 1 * 2) > sum(Distinct U.cnt) and
      (
          t0.id <= t1.subId ||
          t1.id >= 1
          )
   or (
          a = 1 and b = 3 Or
          c = 10 and d != 20
          ) xor
      1 != 2